From 32a5d81e6be1c59006c2939b52f18425a7a30bc8 Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Sat, 4 Apr 2026 14:19:10 +0900
Subject: [PATCH 1/9] Gradual diff calculator

---
 .../Difficulty/DifficultyCalculator.cs        |  22 ++
 .../Difficulty/GradualDifficultyEnumerator.cs | 230 ++++++++++++++++++
 2 files changed, 252 insertions(+)
 create mode 100644 osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs

diff --git a/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs b/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
index 9b28045ed3..2e069cd56b 100644
--- a/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
+++ b/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
@@ -90,6 +90,28 @@ public DifficultyAttributes Calculate([NotNull] IEnumerable<Mod> mods, Cancellat
             return CreateDifficultyAttributes(Beatmap, playableMods, skills);
         }
 
+        public GradualDifficultyEnumerator CreateGradualDifficulty([NotNull] IEnumerable<Mod> mods, CancellationToken cancellationToken = default)
+        {
+            using var timedCancellationSource = new CancellationTokenSource(TimeSpan.FromSeconds(10));
+
+            if (!cancellationToken.CanBeCanceled)
+                cancellationToken = timedCancellationSource.Token;
+
+            cancellationToken.ThrowIfCancellationRequested();
+            preProcess(mods, cancellationToken);
+
+            var skills = CreateSkills(Beatmap, playableMods, clockRate);
+            var difficultyObjects = getDifficultyHitObjects().ToArray();
+            return new GradualDifficultyEnumerator(
+                Beatmap,
+                playableMods,
+                difficultyObjects,
+                skills,
+                clockRate,
+                CreateDifficultyAttributes
+            );
+        }
+
         /// <summary>
         /// Calculates the difficulty of the beatmap with no mods applied and returns a set of <see cref="TimedDifficultyAttributes"/> representing the difficulty at every relevant time value in the beatmap.
         /// </summary>
diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
new file mode 100644
index 0000000000..8328634ac0
--- /dev/null
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -0,0 +1,230 @@
+// Copyright (c) ppy Pty Ltd <contact@ppy.sh>. Licensed under the MIT Licence.
+// See the LICENCE file in the repository root for full licence text.
+
+using System;
+using System.Collections.Generic;
+using System.Linq;
+using System.Threading;
+using osu.Framework.Lists;
+using osu.Game.Beatmaps;
+using osu.Game.Beatmaps.ControlPoints;
+using osu.Game.Beatmaps.Timing;
+using osu.Game.Rulesets.Difficulty.Preprocessing;
+using osu.Game.Rulesets.Difficulty.Skills;
+using osu.Game.Rulesets.Mods;
+using osu.Game.Rulesets.Objects;
+
+namespace osu.Game.Rulesets.Difficulty
+{
+    public sealed class GradualDifficultyEnumerator
+    {
+        private readonly IBeatmap beatmap;
+        private readonly ProgressiveCalculationBeatmap progressiveBeatmap;
+
+        private readonly Mod[] mods;
+
+        private readonly DifficultyHitObject[] difficultyHitObjects;
+        private int difficultyHitObjectCursor = 0;
+
+        private readonly double clockRate;
+
+        private readonly Func<IBeatmap, Mod[], Skill[], double, DifficultyAttributes> createDifficultyAttributes;
+
+        public Skill[] Skills { get; }
+
+        public GradualDifficultyEnumerator(
+            IBeatmap beatmap,
+            Mod[] mods,
+            DifficultyHitObject[] difficultyHitObjects,
+            Skill[] skills,
+            double clockRate,
+            Func<IBeatmap, Mod[], Skill[], double, DifficultyAttributes> createDifficultyAttributes)
+        {
+            this.beatmap = beatmap;
+            progressiveBeatmap = new ProgressiveCalculationBeatmap(beatmap);
+            this.mods = mods;
+            this.difficultyHitObjects = difficultyHitObjects;
+            this.clockRate = clockRate;
+            this.createDifficultyAttributes = createDifficultyAttributes;
+            Skills = skills;
+        }
+
+        public TimedDifficultyAttributes? CalculateNext(CancellationToken cancellationToken = default)
+        {
+            var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
+            if (hitObject == null)
+                return null;
+            progressiveBeatmap.HitObjects.Add(hitObject);
+
+            while (difficultyHitObjectCursor < difficultyHitObjects.Length)
+            {
+                var difficultyHitObject = difficultyHitObjects[difficultyHitObjectCursor];
+                if (difficultyHitObject.BaseObject.GetEndTime() > hitObject.GetEndTime())
+                {
+                    break;
+                }
+                difficultyHitObjectCursor++;
+
+                foreach (var skill in Skills)
+                {
+                    cancellationToken.ThrowIfCancellationRequested();
+                    skill.Process(difficultyHitObject);
+                }
+            }
+
+            return new TimedDifficultyAttributes(
+                hitObject.GetEndTime(),
+                createDifficultyAttributes(progressiveBeatmap, mods, Skills, clockRate)
+            );
+        }
+
+        public void Skip(int offset)
+        {
+            progressiveBeatmap.HitObjects.AddRange(beatmap.HitObjects.Skip(progressiveBeatmap.HitObjects.Count).Take(offset));
+        }
+
+        public void SkipToTime(double time)
+        {
+            while (progressiveBeatmap.HitObjects.Count < beatmap.HitObjects.Count)
+            {
+                var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
+                if (hitObject == null || hitObject.StartTime >= time)
+                    break;
+                progressiveBeatmap.HitObjects.Add(hitObject);
+            }
+        }
+
+        /// <summary>
+        /// Used to calculate timed difficulty attributes, where only a subset of hitobjects should be visible at any point in time.
+        /// </summary>
+        private class ProgressiveCalculationBeatmap : IBeatmap
+        {
+            private readonly IBeatmap baseBeatmap;
+
+            public ProgressiveCalculationBeatmap(IBeatmap baseBeatmap)
+            {
+                this.baseBeatmap = baseBeatmap;
+            }
+
+            public readonly List<HitObject> HitObjects = new List<HitObject>();
+
+            IReadOnlyList<HitObject> IBeatmap.HitObjects => HitObjects;
+
+            #region Delegated IBeatmap implementation
+
+            public BeatmapInfo BeatmapInfo
+            {
+                get => baseBeatmap.BeatmapInfo;
+                set => baseBeatmap.BeatmapInfo = value;
+            }
+
+            public ControlPointInfo ControlPointInfo
+            {
+                get => baseBeatmap.ControlPointInfo;
+                set => baseBeatmap.ControlPointInfo = value;
+            }
+
+            public BeatmapMetadata Metadata => baseBeatmap.Metadata;
+
+            public BeatmapDifficulty Difficulty
+            {
+                get => baseBeatmap.Difficulty;
+                set => baseBeatmap.Difficulty = value;
+            }
+
+            public SortedList<BreakPeriod> Breaks
+            {
+                get => baseBeatmap.Breaks;
+                set => baseBeatmap.Breaks = value;
+            }
+
+            public List<string> UnhandledEventLines => baseBeatmap.UnhandledEventLines;
+
+            public double TotalBreakTime => baseBeatmap.TotalBreakTime;
+            public IEnumerable<BeatmapStatistic> GetStatistics() => baseBeatmap.GetStatistics();
+            public double GetMostCommonBeatLength() => baseBeatmap.GetMostCommonBeatLength();
+            public int BeatmapVersion => baseBeatmap.BeatmapVersion;
+            public IBeatmap Clone() => new ProgressiveCalculationBeatmap(baseBeatmap.Clone());
+
+            public double AudioLeadIn
+            {
+                get => baseBeatmap.AudioLeadIn;
+                set => baseBeatmap.AudioLeadIn = value;
+            }
+
+            public float StackLeniency
+            {
+                get => baseBeatmap.StackLeniency;
+                set => baseBeatmap.StackLeniency = value;
+            }
+
+            public bool SpecialStyle
+            {
+                get => baseBeatmap.SpecialStyle;
+                set => baseBeatmap.SpecialStyle = value;
+            }
+
+            public bool LetterboxInBreaks
+            {
+                get => baseBeatmap.LetterboxInBreaks;
+                set => baseBeatmap.LetterboxInBreaks = value;
+            }
+
+            public bool WidescreenStoryboard
+            {
+                get => baseBeatmap.WidescreenStoryboard;
+                set => baseBeatmap.WidescreenStoryboard = value;
+            }
+
+            public bool EpilepsyWarning
+            {
+                get => baseBeatmap.EpilepsyWarning;
+                set => baseBeatmap.EpilepsyWarning = value;
+            }
+
+            public bool SamplesMatchPlaybackRate
+            {
+                get => baseBeatmap.SamplesMatchPlaybackRate;
+                set => baseBeatmap.SamplesMatchPlaybackRate = value;
+            }
+
+            public double DistanceSpacing
+            {
+                get => baseBeatmap.DistanceSpacing;
+                set => baseBeatmap.DistanceSpacing = value;
+            }
+
+            public int GridSize
+            {
+                get => baseBeatmap.GridSize;
+                set => baseBeatmap.GridSize = value;
+            }
+
+            public double TimelineZoom
+            {
+                get => baseBeatmap.TimelineZoom;
+                set => baseBeatmap.TimelineZoom = value;
+            }
+
+            public CountdownType Countdown
+            {
+                get => baseBeatmap.Countdown;
+                set => baseBeatmap.Countdown = value;
+            }
+
+            public int CountdownOffset
+            {
+                get => baseBeatmap.CountdownOffset;
+                set => baseBeatmap.CountdownOffset = value;
+            }
+
+            public int[] Bookmarks
+            {
+                get => baseBeatmap.Bookmarks;
+                set => baseBeatmap.Bookmarks = value;
+            }
+
+            #endregion
+        }
+    }
+}
-- 
2.33.0.windows.2


From 651cd9e589b9a1b072b1dad7d99f7eea860851de Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Sat, 4 Apr 2026 20:57:13 +0900
Subject: [PATCH 2/9] Split CreateDifficultyAttributes

---
 .../Difficulty/GradualDifficultyEnumerator.cs      | 14 ++++++++------
 1 file changed, 8 insertions(+), 6 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index 8328634ac0..f2574061f7 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -49,11 +49,11 @@ public sealed class GradualDifficultyEnumerator
             Skills = skills;
         }
 
-        public TimedDifficultyAttributes? CalculateNext(CancellationToken cancellationToken = default)
+        public bool MoveNext(CancellationToken cancellationToken = default)
         {
             var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
             if (hitObject == null)
-                return null;
+                return false;
             progressiveBeatmap.HitObjects.Add(hitObject);
 
             while (difficultyHitObjectCursor < difficultyHitObjects.Length)
@@ -72,10 +72,12 @@ public sealed class GradualDifficultyEnumerator
                 }
             }
 
-            return new TimedDifficultyAttributes(
-                hitObject.GetEndTime(),
-                createDifficultyAttributes(progressiveBeatmap, mods, Skills, clockRate)
-            );
+            return true;
+        }
+
+        public DifficultyAttributes CreateDifficultyAttributes()
+        {
+            return createDifficultyAttributes(progressiveBeatmap, mods, Skills, clockRate);
         }
 
         public void Skip(int offset)
-- 
2.33.0.windows.2


From a1e4987bcacef08abe4a237742c0a0a0181427d7 Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Sat, 4 Apr 2026 22:55:51 +0900
Subject: [PATCH 3/9] Add updateDifficultyHitObjects for handling on advance
 and skip

---
 .../Difficulty/GradualDifficultyEnumerator.cs | 40 ++++++++++++++-----
 1 file changed, 29 insertions(+), 11 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index f2574061f7..3f100118e4 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -49,17 +49,16 @@ public sealed class GradualDifficultyEnumerator
             Skills = skills;
         }
 
-        public bool MoveNext(CancellationToken cancellationToken = default)
+        private void updateDifficultyHitObjects(CancellationToken cancellationToken)
         {
-            var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
-            if (hitObject == null)
-                return false;
-            progressiveBeatmap.HitObjects.Add(hitObject);
+            if (beatmap.HitObjects.Count <= progressiveBeatmap.HitObjects.Count)
+                return;
 
+            var lastHitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
             while (difficultyHitObjectCursor < difficultyHitObjects.Length)
             {
                 var difficultyHitObject = difficultyHitObjects[difficultyHitObjectCursor];
-                if (difficultyHitObject.BaseObject.GetEndTime() > hitObject.GetEndTime())
+                if (difficultyHitObject.BaseObject.GetEndTime() > lastHitObject.GetEndTime())
                 {
                     break;
                 }
@@ -71,8 +70,6 @@ public bool MoveNext(CancellationToken cancellationToken = default)
                     skill.Process(difficultyHitObject);
                 }
             }
-
-            return true;
         }
 
         public DifficultyAttributes CreateDifficultyAttributes()
@@ -80,20 +77,41 @@ public DifficultyAttributes CreateDifficultyAttributes()
             return createDifficultyAttributes(progressiveBeatmap, mods, Skills, clockRate);
         }
 
-        public void Skip(int offset)
+        public bool Advance(CancellationToken cancellationToken = default)
+        {
+            if (beatmap.HitObjects.Count <= progressiveBeatmap.HitObjects.Count)
+                return false;
+
+            var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
+            progressiveBeatmap.HitObjects.Add(hitObject);
+            updateDifficultyHitObjects(cancellationToken);
+
+            return true;
+        }
+
+        public void Skip(int offset, CancellationToken cancellationToken = default)
         {
             progressiveBeatmap.HitObjects.AddRange(beatmap.HitObjects.Skip(progressiveBeatmap.HitObjects.Count).Take(offset));
+            updateDifficultyHitObjects(cancellationToken);
         }
 
-        public void SkipToTime(double time)
+        public void SkipToTime(double time, CancellationToken cancellationToken = default)
         {
             while (progressiveBeatmap.HitObjects.Count < beatmap.HitObjects.Count)
             {
                 var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
-                if (hitObject == null || hitObject.StartTime >= time)
+                if (hitObject.StartTime >= time)
                     break;
                 progressiveBeatmap.HitObjects.Add(hitObject);
             }
+
+            updateDifficultyHitObjects(cancellationToken);
+        }
+
+        public void SkipToEnd(CancellationToken cancellationToken = default)
+        {
+            progressiveBeatmap.HitObjects.AddRange(beatmap.HitObjects.Skip(progressiveBeatmap.HitObjects.Count));
+            updateDifficultyHitObjects(cancellationToken);
         }
 
         /// <summary>
-- 
2.33.0.windows.2


From d015701d87b58f11b4a7ba15a22d6bc9cfa48b17 Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Sat, 4 Apr 2026 23:02:03 +0900
Subject: [PATCH 4/9] Fix update by clamping to last hit object

---
 osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index 3f100118e4..e01592e26d 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -51,10 +51,10 @@ public sealed class GradualDifficultyEnumerator
 
         private void updateDifficultyHitObjects(CancellationToken cancellationToken)
         {
-            if (beatmap.HitObjects.Count <= progressiveBeatmap.HitObjects.Count)
-                return;
+            var lastHitObject = beatmap.HitObjects.Count <= progressiveBeatmap.HitObjects.Count
+                ? beatmap.HitObjects[beatmap.HitObjects.Count - 1]
+                : beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
 
-            var lastHitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
             while (difficultyHitObjectCursor < difficultyHitObjects.Length)
             {
                 var difficultyHitObject = difficultyHitObjects[difficultyHitObjectCursor];
-- 
2.33.0.windows.2


From 24c6d721a3ce430d4b276ae5e0078c79864c5f4c Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Thu, 9 Apr 2026 17:06:18 +0900
Subject: [PATCH 5/9] Fix gradual last object offset

---
 osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs | 6 +++---
 1 file changed, 3 insertions(+), 3 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index e01592e26d..f7d4286de2 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -51,10 +51,10 @@ public sealed class GradualDifficultyEnumerator
 
         private void updateDifficultyHitObjects(CancellationToken cancellationToken)
         {
-            var lastHitObject = beatmap.HitObjects.Count <= progressiveBeatmap.HitObjects.Count
-                ? beatmap.HitObjects[beatmap.HitObjects.Count - 1]
-                : beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
+            if (progressiveBeatmap.HitObjects.Count == 0)
+                return;
 
+            var lastHitObject = progressiveBeatmap.HitObjects[progressiveBeatmap.HitObjects.Count - 1];
             while (difficultyHitObjectCursor < difficultyHitObjects.Length)
             {
                 var difficultyHitObject = difficultyHitObjects[difficultyHitObjectCursor];
-- 
2.33.0.windows.2


From 95aca79e0c4f4b960bb795a7ddaf8fae0a8bf5b7 Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Thu, 21 May 2026 13:22:49 +0900
Subject: [PATCH 6/9] Remove UnhandledEventLines

* Removed in latest upstream
---
 osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs | 2 --
 1 file changed, 2 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index f7d4286de2..c804f2d812 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -158,8 +158,6 @@ public SortedList<BreakPeriod> Breaks
                 set => baseBeatmap.Breaks = value;
             }
 
-            public List<string> UnhandledEventLines => baseBeatmap.UnhandledEventLines;
-
             public double TotalBreakTime => baseBeatmap.TotalBreakTime;
             public IEnumerable<BeatmapStatistic> GetStatistics() => baseBeatmap.GetStatistics();
             public double GetMostCommonBeatLength() => baseBeatmap.GetMostCommonBeatLength();
-- 
2.33.0.windows.2


From e031c4b9bdb2a31e6e6db1b5b6ecad08191b54be Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Mon, 15 Jun 2026 21:31:01 +0900
Subject: [PATCH 7/9] Expose progressive beatmap

---
 osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index c804f2d812..4fb6a5693c 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -21,6 +21,8 @@ public sealed class GradualDifficultyEnumerator
         private readonly IBeatmap beatmap;
         private readonly ProgressiveCalculationBeatmap progressiveBeatmap;
 
+        public IBeatmap ProgressiveBeatmap => progressiveBeatmap;
+
         private readonly Mod[] mods;
 
         private readonly DifficultyHitObject[] difficultyHitObjects;
-- 
2.33.0.windows.2


From 38c147af002fa21e373073eda9b93091f7cabcd5 Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Tue, 16 Jun 2026 13:57:42 +0900
Subject: [PATCH 8/9] Fix SkipToTime to correctly skip only ended objects

---
 .../Rulesets/Difficulty/GradualDifficultyEnumerator.cs    | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index 4fb6a5693c..b0665e1341 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -97,17 +97,21 @@ public void Skip(int offset, CancellationToken cancellationToken = default)
             updateDifficultyHitObjects(cancellationToken);
         }
 
-        public void SkipToTime(double time, CancellationToken cancellationToken = default)
+        public int SkipToTime(double time, CancellationToken cancellationToken = default)
         {
+            int added = 0;
             while (progressiveBeatmap.HitObjects.Count < beatmap.HitObjects.Count)
             {
                 var hitObject = beatmap.HitObjects[progressiveBeatmap.HitObjects.Count];
-                if (hitObject.StartTime >= time)
+                if (hitObject.GetEndTime() > time)
                     break;
+
                 progressiveBeatmap.HitObjects.Add(hitObject);
+                added++;
             }
 
             updateDifficultyHitObjects(cancellationToken);
+            return added;
         }
 
         public void SkipToEnd(CancellationToken cancellationToken = default)
-- 
2.33.0.windows.2


From ba8bf8f1ee080651532201ef3236d47f68b2cbfd Mon Sep 17 00:00:00 2001
From: storycraft <storycraft@pancake.sh>
Date: Sun, 21 Jun 2026 13:27:14 +0900
Subject: [PATCH 9/9] remove obsolete clockRate

---
 osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs      | 3 +--
 .../Rulesets/Difficulty/GradualDifficultyEnumerator.cs    | 8 +++-----
 2 files changed, 4 insertions(+), 7 deletions(-)

diff --git a/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs b/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
index 2e069cd56b..98965bad47 100644
--- a/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
+++ b/osu.Game/Rulesets/Difficulty/DifficultyCalculator.cs
@@ -100,14 +100,13 @@ public GradualDifficultyEnumerator CreateGradualDifficulty([NotNull] IEnumerable
             cancellationToken.ThrowIfCancellationRequested();
             preProcess(mods, cancellationToken);
 
-            var skills = CreateSkills(Beatmap, playableMods, clockRate);
+            var skills = CreateSkills(Beatmap, playableMods);
             var difficultyObjects = getDifficultyHitObjects().ToArray();
             return new GradualDifficultyEnumerator(
                 Beatmap,
                 playableMods,
                 difficultyObjects,
                 skills,
-                clockRate,
                 CreateDifficultyAttributes
             );
         }
diff --git a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
index b0665e1341..ee539d9756 100644
--- a/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
+++ b/osu.Game/Rulesets/Difficulty/GradualDifficultyEnumerator.cs
@@ -30,7 +30,7 @@ public sealed class GradualDifficultyEnumerator
 
         private readonly double clockRate;
 
-        private readonly Func<IBeatmap, Mod[], Skill[], double, DifficultyAttributes> createDifficultyAttributes;
+        private readonly Func<IBeatmap, Mod[], Skill[], DifficultyAttributes> createDifficultyAttributes;
 
         public Skill[] Skills { get; }
 
@@ -39,14 +39,12 @@ public sealed class GradualDifficultyEnumerator
             Mod[] mods,
             DifficultyHitObject[] difficultyHitObjects,
             Skill[] skills,
-            double clockRate,
-            Func<IBeatmap, Mod[], Skill[], double, DifficultyAttributes> createDifficultyAttributes)
+            Func<IBeatmap, Mod[], Skill[], DifficultyAttributes> createDifficultyAttributes)
         {
             this.beatmap = beatmap;
             progressiveBeatmap = new ProgressiveCalculationBeatmap(beatmap);
             this.mods = mods;
             this.difficultyHitObjects = difficultyHitObjects;
-            this.clockRate = clockRate;
             this.createDifficultyAttributes = createDifficultyAttributes;
             Skills = skills;
         }
@@ -76,7 +74,7 @@ private void updateDifficultyHitObjects(CancellationToken cancellationToken)
 
         public DifficultyAttributes CreateDifficultyAttributes()
         {
-            return createDifficultyAttributes(progressiveBeatmap, mods, Skills, clockRate);
+            return createDifficultyAttributes(progressiveBeatmap, mods, Skills);
         }
 
         public bool Advance(CancellationToken cancellationToken = default)
-- 
2.33.0.windows.2

